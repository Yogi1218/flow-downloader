services:
  - type: web
    name: flow-downloader
    runtime: docker
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 8080
    disk:
      name: flow-downloads-disk
      mountPath: /app/downloads
      sizeGB: 1
